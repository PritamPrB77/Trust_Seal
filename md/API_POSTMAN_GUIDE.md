# API Postman Guide

The maintained Postman guide is now in:

- [`postmanGuide.md`](../postmanGuide.md)

It reflects the current schema, role permissions, telemetry endpoints, and the new agentic RAG endpoints:

- `POST /api/v1/ingest`
- `POST /api/v1/chat`
- `GET /health`
